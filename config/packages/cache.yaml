framework:
    cache:
        pools:
            meteo.cache:
                adapter: cache.adapter.memcached
                provider: 'memcached://127.0.0.1:11211'
                default_lifetime: 3600  # 1 heure

